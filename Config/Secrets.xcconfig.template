// Polli-Nation local Xcode settings
// Government-only mode does not require paid or third-party API keys.

// Optional: route requests through your VPS backend. Leave blank to call NOAA/NWS directly from iOS.
POLLEN_BACKEND_BASE_URL =
